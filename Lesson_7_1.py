"""
Задание 1
📌 Создайте функцию, которая удаляет из текста все символы
кроме букв латинского алфавита и пробелов.
📌 Возвращается строка в нижнем регистре.
"""

import re

def clean_text(text: str) -> str:
    """
    Удаляет из текста все символы, кроме букв латинского алфавита и пробелов,
    и возвращает результат в нижнем регистре.
    """
    # Удаляем всё, что не латинская буква (A–Z, a–z) и не пробел
    cleaned = re.sub(r'[^A-Za-z ]+', '', text)
    # Переводим в нижний регистр
    return cleaned.lower()

# Пример использования
if __name__ == "__main__":
    sample = "Hello, World! Привет, мир! 12345 -- TEST."
    result = clean_text(sample)
    print(result)  # hello world  test
