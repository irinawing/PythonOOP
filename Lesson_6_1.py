"""
Задание 91.
📌 Создайте функцию, которая запрашивает числовые данные от
пользователя до тех пор, пока он не введёт целое или
вещественное число.
📌 Обрабатывайте не числовые данные как исключения.
"""

def get_number_input():
    while True:
        user_input = input("Введите целое или вещественное число: ")
        try:
            # Пробуем преобразовать в целое число
            return int(user_input)
        except ValueError:
            try:
                # Если не получилось как целое, пробуем как вещественное
                return float(user_input)
            except ValueError:
                print("Ошибка: введённые данные не являются числом. Попробуйте ещё раз.")

# Пример использования
if __name__ == "__main__":
    number = get_number_input()
    print(f"Вы ввели число: {number}")